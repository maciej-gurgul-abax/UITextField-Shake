import XCTest
@testable import UITextField_Shake

final class UITextField_ShakeTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(UITextField_Shake().text, "Hello, World!")
    }
}
